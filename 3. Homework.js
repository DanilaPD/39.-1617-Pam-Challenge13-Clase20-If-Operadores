let a = 25;
let b = 30;

if (a < b) {
  console.log(`${a} es menor a ${b}.`);
} else if (a > b) {
  console.log(`${a} es mayor a ${b}.`);
} else {
  console.log(`${a} es igual a ${b}.`);
}

//***

let c = "¡Hola!";
let d = "¡Nos estamos viendo!";
let e = "Take care.";

if (c < d) {
  console.log(`Decimos "${c}" en vez de "${d}".`);
} else if (c > d) {
  console.log(`Decimos "${d}" en vez de "${c}".`);
} else {
  console.log(`We don´t care, por lo que decimos "${e}".`);
}
